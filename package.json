{
  "name": "socket-test",
  "version": "1.0.0",
  "description": "just wanna try to test socket",
  "main": "server.js",
  "author": "ssoda",
  "license": "MIT",
  "scripts": {
    "start": "node server.js",
    "test": "./node_modules/mocha/bin/mocha test/* --timeout 10000 --exit"
  },
  "dependencies": {
    "socket.io": "^2.4.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^6.1.4",
    "socket.io-client": "^2.2.0"
  }
}
